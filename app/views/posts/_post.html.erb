<div id="<%= dom_id post %>">
  <h2><%= post.title %></h2>
  <h4>posted by <%= post.spree_user_id %></h4>
  <h6><%= pluralize(post.views, "view")  %></h6>
  <div>
    <%= post.body %>
  </div>

</div>
